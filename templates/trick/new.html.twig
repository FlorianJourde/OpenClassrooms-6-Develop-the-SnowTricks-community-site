{% extends 'base.html.twig' %}

{% block title %}Ajouter un trick{% endblock %}

{% block body %}

    {% include 'partials/banner.html.twig' with { background_image: 'mountains-1.jpg', title: block('title') } %}

    <section class="edit-section">
        <div class="wrapper wrapper-small">

        <div class="actions">
            <a class="button-primary button-black" href="{{ path('app_trick_index') }}"><i class="fas fa-arrow-left"></i>Retour</a>
        </div>

        {{ include('trick/_trick_form.html.twig', {'button_label': 'Ajouter'}) }}

        </div>
    </section>

{% endblock %}
