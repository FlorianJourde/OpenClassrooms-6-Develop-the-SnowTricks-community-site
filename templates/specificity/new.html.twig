{% extends 'base.html.twig' %}

{% block title %}Ajouter un descriptif technique{% endblock %}

{% block body %}

    {% set actions %}
        <section class="actions-section">
            <div class="actions">
                <a class="button-primary button-black" href="{{ path('app_specificity_index') }}"><i class="fas fa-arrow-left"></i>Retour</a>
            </div>
        </section>
    {% endset %}

    {% include 'partials/banner.html.twig' with { background_image: 'mountains-4.jpg', title: block('title') } %}

    <section class="edit-section section-spacing">
        <div class="wrapper wrapper-small">
            {{ include('specificity/_specificity_form.html.twig', {'button_label': 'Ajouter'}) }}
        </div>
    </section>

{% endblock %}
