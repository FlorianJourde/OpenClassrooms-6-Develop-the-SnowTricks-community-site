<form method="post" action="{{ path('app_specificity_delete', {'id': specificity.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer ce descriptif technique ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ specificity.id) }}">
    <button class="button-primary button-dark-red"><i class="fas fa-trash-alt"></i> Supprimer</button>
</form>
