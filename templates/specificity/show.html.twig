{% extends 'base.html.twig' %}

{% block title %}Specificity{% endblock %}

{% block body %}
    <h1>Specificity</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ specificity.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ specificity.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ specificity.description }}</td>
            </tr>
            <tr>
                <th>Icon</th>
                <td>{{ specificity.icon }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_specificity_index') }}">back to list</a>

    <a href="{{ path('app_specificity_edit', {'id': specificity.id}) }}">edit</a>

    {{ include('specificity/_delete_form.html.twig') }}
{% endblock %}
